pipeline {
  agent any
  stages {
    stage('Secrets scan') {
      steps {
        sh 'trufflehog --regex --entropy False https://github.com/rodrigolinux30/cyber_security_advanced_jenkin'
      }
      stage('Proof of concept') {
      steps {
        sh 'trufflehog --regex --entropy False https://github.com/webpwnized/mutillidae'
      }
    }
   }
  }
