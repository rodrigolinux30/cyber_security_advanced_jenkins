pipeline {
    agent any
    stages {
        stage('Build')
        {
            steps 
            {
                sh '''#!/bin/bash
                echo "This pipeline is running as: " && whoami
                '''
            }
        }
        }
    }
